import { FC } from "react";
import { Button } from "react-bootstrap";
// import { } from "../../../types";

const Btn: FC = (
  {
    //   id,
    //   title,
    //   img,
    //   description,
    //   category,
  }
) => {
  return (
    <Button className="text-center">
      Btn
    </Button>
  );
};

export { Btn };
