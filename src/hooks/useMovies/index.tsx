import { useContext } from "react"
import { StoreContext} from "../../contexts"

const useMovies = () => {

    //getMovie para
    // const { users } = useContext(StoreContext)
    // return { users }
}

export {useMovies}